/*
package com.src.presentation

import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.navigation.NavHost
import androidx.navigation.NavHostController
import com.src.presentation.views.splash_first.SplashFirstView
import com.src.presentation.views.splash_location.SplashLocationView

@Composable
fun Navigation(
    navController: NavHostController,
    modifier: Modifier,
) {
    NavHost(navController, startDestination = Screen.SplashFirstView.route, modifier) {

        composable(Screen.SplashFirstView.route) {
            SplashFirstView(navController)
        }

        composable(Screen.SplashLocationView.route) {
            SplashLocationView()
        }

    }
}*/
